# Branding Assets

## GlyphOS Encoding Demo

Assets:
- `glyphos-encoding-demo.gif`
- `glyphos-encoding-demo-380w.gif`
- `glyphos-encoding-demo-poster.png`

Alt text:
> Animated demo showing a verbose local AI request being converted into compact GlyphOS tokens, reducing the displayed payload by 60-90%, then routing through a local llama.cpp endpoint at 127.0.0.1:8081/v1. The final frame reads: "Stay local. Stay private. Stay fast."

Short description:
> GlyphOS AI Compute compresses supported local AI workloads into compact glyph payloads, routes them through llama.cpp, and keeps the workflow local and private.

Website embed asset:
> Use `glyphos-encoding-demo-380w.gif` for compact website placements. It is 380x214 and preserves the original 16:9 aspect ratio.
