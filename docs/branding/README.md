# Branding Assets

## GlyphOS Encoding Demo

Assets:
- `glyphos-encoding-demo.gif`
- `glyphos-encoding-demo-380w.gif`
- `glyphos-encoding-demo-poster.png`

Alt text:
> Animated demo showing a verbose local AI request being converted into compact GlyphOS tokens, reducing the displayed payload by 60-90%, then routing through a local llama.cpp endpoint at 127.0.0.1:8081/v1. The final frame reads: "Stay local. Stay private. Stay fast."

Short description:
> GlyphOS AI Compute compresses supported local AI workloads into compact glyph payloads, routes them through llama.cpp, and keeps the workflow local and private.

Website embed asset:
> Use `glyphos-encoding-demo-380w.gif` for compact website placements. It is 380x214 and preserves the original 16:9 aspect ratio.

## Sovereignty Bridge Clip

Assets:
- `sovereignty-bridge.gif`
- `sovereignty-bridge-poster.png`

Alt text:
> Animated split-screen demo showing a Claude Code terminal starting the local Claude gateway while the llama-model-manager dashboard turns green. A local repository explanation request triggers dashboard CPU, GPU, and VRAM activity, then a Glyph Encoding Active overlay shows up to 90% payload reduction and local routing before the model switches to Gemma-4 E4B. The final frame reads: "IP: PROTECTED. BRAIN: LOCAL." and shows soulhash.ai/downloads.

Short description:
> The Sovereignty Bridge clip shows Claude Code routing through a local gateway into llama.cpp, with llama-model-manager proving local health, model switching, and GlyphOS payload reduction.
